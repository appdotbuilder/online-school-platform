export async function deactivateEnrollment(courseId: number, studentId: number): Promise<boolean> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is deactivating (unenrolling) a student from a course.
    // Should set is_active to false instead of deleting the record for historical purposes.
    // Returns true if deactivation was successful, false otherwise.
    return false;
}